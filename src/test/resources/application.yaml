spring:
  application:
    name: MinchevParquet
  jpa:
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: never

parquets.retention.period: P900D

jwt:
  secret: test

